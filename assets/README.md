# assets

Posto dei modelli. Oggi vuoto.

- `vox/`  MagicaVoxel e riferimenti a cubetti
- `gltf/` .glb / .gltf (Blender o altro)

Il catalogo (`engine/catalog.mjs`, quando cՏ) punta qui con `mesh` + `asset`. Stesso id mezzo, cubetti o file sagomato.
